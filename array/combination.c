#include<stdio.h>
int main()
{
	int a[5],len,i;
	len=sizeof(a)/sizeof(a[0]);
	printf("enter elements");
	for(i=0;i<len;i++)
	{
		scanf("%d",a[i]);
	}
	for(i=0;i<len;i++)
	{
		printf("%d\t",a[i]);
	}
	printf("combination form by the given number is:");
	scanf("%d",&r);
	
		if(ndex==r)
		{
			
		}
	}
}
